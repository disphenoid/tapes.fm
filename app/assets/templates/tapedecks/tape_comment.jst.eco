  <span class="user_name">
  <%= @model.get("user_name")%>
  </span>
  <span title="<%= if (@model.get("created_at") == undefined) then (new Date).toDateString() else @model.get("created_at") %>" class="date">
    <%= if (@model.get("created_at") == undefined) then (new Date).toDateString() else @model.get("created_at") %>
  </span>
  <br />
  <span class="comment_body">
  <%= @model.get("body")%>
  </span>
